# Configuración para Cloudflare
# Reemplaza con la URL de tu túnel de Cloudflare del backend

# URL completa del backend en Cloudflare
VITE_API_BASE_URL=https://tu-backend-tunel.trycloudflare.com

# Para el proxy (no se usa en Cloudflare, pero lo dejamos por compatibilidad)
VITE_BACKEND_URL=https://tu-backend-tunel.trycloudflare.com
